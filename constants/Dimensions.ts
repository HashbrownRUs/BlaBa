//Temp
const textBoxHeight = 40; //textbox in message.tsx

export const textBox = {
    Height: textBoxHeight,
    Radius: textBoxHeight / 2,
    BackGHeight: textBoxHeight + 10,
    BackGRadius: (textBoxHeight + 10)/ 2,
    bottom: 10,
}

const boxHeight = 90; //contacts in home.tsx currently 

export const contactBox = {
    boxHeight: 90,
    profileHeight: boxHeight-10,
}